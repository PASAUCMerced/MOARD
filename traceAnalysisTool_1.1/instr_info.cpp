#include <iostream>  
using namespace std;

#include "instr_info.h"

void printLine1()
{
	/*
	int i = app_trace.size();
	cout<<app_trace[i-1].line0_blockId<<" "<<app_trace[i-1].line0_lineId<<" "<<app_trace[i-1].line0_funcName<<" "<<
		app_trace[i-1].line0_basicBlockId<<" "<<app_trace[i-1].line0_staticInstId<<" "<<app_trace[i-1].line0_opcodeId<<" "<<
		app_trace[i-1].line0_dynInstId<<'\n'<<endl;
		*/
}

instrInfo::instrInfo()
{
}

instrInfo::~instrInfo()
{
}
